# wuzzler.github.io

## initial setup 

see `setup.sh` and install GemFiles afterwards.

## GemFile

```bash
# if new gems are added:
bundle install
```

## start local server

```bash
bundle exec jekyll serve
```